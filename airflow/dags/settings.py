"""
Módulo DEPRECADO: Antes estava aqui uma cópia de configurações.
Use `from config.settings import ...` no lugar deste arquivo.

Este arquivo permanece apenas para compatibilidade retroativa; não o
utilize em código novo. Ele levantará uma ImportError para forçar o uso
do arquivo central `config/settings.py`.
"""
raise ImportError("Use 'config.settings' em vez de 'airflow.dags.settings'.")
